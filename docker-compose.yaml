  services:
    app:
      build:
        context: .
        dockerfile: Dockerfile
      container_name: nextjs-app
      restart: unless-stopped
      ports:
        - "3030:3030"
      environment:
        - NODE_ENV=production
